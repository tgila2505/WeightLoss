{
  "name": "@weightloss/shared",
  "private": true,
  "scripts": {
    "lint": "node -e \"console.log('No shared source files to lint.')\"",
    "test": "node -e \"console.log('No shared tests configured yet.')\"",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
