# Shared

- Reserved for future shared TypeScript utilities, contracts, and types.
- Do not place runtime environment-variable reads in this package.
- Run `pnpm lint` and `pnpm format` from this directory after workspace install.
