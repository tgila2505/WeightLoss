# Frontend

- Next.js App Router frontend.
- Read environment variables through `process.env`.
- Use `NEXT_PUBLIC_*` only for values intentionally exposed to the browser.
- Store the backend JWT in `localStorage` under `access_token` for authenticated API requests.
- Run `pnpm dev` from this directory after installing dependencies.
