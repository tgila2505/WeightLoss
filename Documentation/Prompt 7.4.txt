You are a senior software engineer.

Your task is to implement a production-ready feature with clean structure, minimal complexity, and strict adherence to requirements.

You MUST think through the full system before writing code.

Do NOT overengineer. Do NOT introduce features not explicitly required.

INPUT

Task:
Implement observability and monitoring capabilities across the system, including centralized logging, error tracking, performance monitoring, alerting, and SLA threshold definitions.

Expected Outcome:

Logging:

Centralized logging mechanism for backend (structured logs)
Consistent log format across services (timestamp, level, context, request ID)
Ability to trace requests across the system

Error Tracking:

Capture and log application errors with meaningful context
Differentiate between handled vs unhandled errors
Minimal integration (no heavy external dependency unless lightweight and optional)

Performance Monitoring:

Track API response times
Measure key operation latencies (e.g., plan generation, onboarding)
Basic metrics collection (request count, error rate, latency)

Alerting:

Alerts triggered based on defined conditions (e.g., high error rate, slow responses)
Simple alerting mechanism (log-based or threshold-based; no heavy infra)

SLA Thresholds:

Define and enforce SLA thresholds (e.g., response time limits, uptime assumptions)
Configurable thresholds in a central location

System-wide:

No breaking changes to existing functionality
Minimal overhead added to system performance
Easy to extend or integrate with external monitoring tools later
Clean and maintainable structure

Architecture Constraints:

Backend: FastAPI (Python)
Frontend: React (no heavy monitoring SDKs required on frontend unless minimal)
Prefer built-in logging or lightweight libraries (e.g., Python logging module)
Avoid introducing full observability stacks (e.g., ELK, Prometheus) unless mocked or minimally abstracted
Keep implementation environment-agnostic

(Optional) Existing Implementation:

Basic logging may exist but is inconsistent or unstructured
No centralized logging format
No formal error tracking system
No performance metrics tracking
No alerting or SLA definitions currently implemented

EXECUTION RULES

Only implement what is explicitly required
Do NOT add unrelated features (e.g., full monitoring infra, dashboards, external SaaS configs)
Avoid unnecessary tools, frameworks, or abstractions
Keep implementation minimal but correct
Prefer convention over configuration
Ensure fast developer onboarding (low setup complexity)

IMPLEMENTATION STRATEGY (internal reasoning, do NOT output)

Standardize logging format and create centralized logger utility
Add request-level context (e.g., request ID)
Implement error capturing and classification
Add performance timing middleware for APIs
Define simple in-app metrics collection
Implement threshold-based alert triggers
Create centralized SLA configuration
Validate minimal overhead and correctness

OUTPUT FORMAT

You MUST return output in a structured, file-based format.

First, output the file tree:

/root
├── file-or-folder
├── ...

Then provide file contents:

=== FILE: path/to/file ===
<code>

Repeat for ALL necessary files.

Do NOT skip important files.
Do NOT inline explanations inside code.

QUALITY CHECK (MANDATORY — perform before final answer)

Does this fully satisfy the Task and Expected Outcome?
Is the implementation minimal and not overengineered?
Are all components runnable with minimal setup?
Are dependencies justified and minimal?
Is separation of concerns maintained?
Are there any missing configs or broken links between modules?

If issues exist → fix them before returning.

OUTPUT

Implementation:
<complete file tree + code>

Key Decisions:

Only high-impact design decisions
Keep concise

Assumptions:

Only if critical and unavoidable

Confidence:
HIGH | MEDIUM | LOW