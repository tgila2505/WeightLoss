You are a senior software engineer.

Your task is to implement a production-ready feature with clean structure, minimal complexity, and strict adherence to requirements.

You MUST think through the full system before writing code.

Do NOT overengineer. Do NOT introduce features not explicitly required.

---

INPUT

Task:
Implement the core graph foundation for the Personal Profile Mind Map engine.

You MUST execute the following steps **in strict sequence**, as later steps depend on earlier definitions.

---

Step 6.1.1.1 — Define Node Interface:

* Define a Node structure representing a mind map element
* Include minimal required fields:

  * id
  * label
  * type (optional)
  * metadata (optional)
* Keep interface generic and extensible

---

Step 6.1.1.2 — Define Edge Structure:

* Define Edge structure connecting nodes
* Include:

  * source
  * target
  * type (optional)
* Keep minimal

---

Step 6.1.1.3 — Define Tree → Graph Transformer:

* Accept hierarchical (tree) input
* Convert into flat graph:

  * list of nodes
  * list of edges
* Use simple traversal (e.g., DFS)
* No complex transformations

---

Step 6.1.2.1 — Create Static JSON Schema:

* Define a static JSON structure representing a sample profile tree
* Include nested nodes (parent → child relationships)
* Keep structure simple and readable

---

Step 6.1.2.2 — Generate Initial Nodes:

* Use transformer to extract nodes from JSON
* Ensure unique IDs
* No deduplication logic beyond basics

---

Step 6.1.2.3 — Generate Initial Edges:

* Generate edges from parent-child relationships
* Ensure all edges reference valid node IDs

---

Step 6.1.2.4 — Layout Algorithm:

* Implement simple layout logic:

  * assign x/y positions to nodes
  * use deterministic layout (e.g., tree layout or grid)
* No external graph libraries
* Keep algorithm minimal and predictable

---

Expected Outcome:

* Node and Edge interfaces defined
* Tree → graph transformer works correctly
* Static JSON schema exists and is valid
* Nodes and edges generated from JSON
* Layout assigns positions to nodes
* Output graph is structured and usable by frontend
* No visualization or rendering included
* No external graph libraries used

---

Architecture Constraints:

* Must be implemented in frontend (or shared module if applicable)
* Use TypeScript for interfaces and logic
* Do NOT introduce graph libraries (e.g., D3, Cytoscape)
* Do NOT implement UI rendering
* Keep everything deterministic and simple
* Maintain separation:

  * data structures
  * transformation logic
  * layout logic

---

(Optional) Existing Implementation:

* No graph engine exists yet
* Mind map feature is being introduced from scratch

---

EXECUTION RULES

* Execute steps strictly in order
* Only implement what is explicitly required
* Do NOT add features (e.g., drag/drop, interactivity, styling)
* Avoid unnecessary abstractions
* Keep implementation minimal but correct
* Prefer clarity over flexibility

---

IMPLEMENTATION STRATEGY (internal reasoning, do NOT output)

1. Define Node interface
2. Define Edge structure
3. Implement tree-to-graph transformer
4. Create static JSON schema
5. Generate nodes
6. Generate edges
7. Implement layout algorithm
8. Validate output consistency

---

OUTPUT FORMAT

You MUST return output in a structured, file-based format.

First, output the file tree:

/root
├── frontend/
│   └── lib/
│       ├── graph/
│       │   ├── types.ts
│       │   ├── transformer.ts
│       │   ├── layout.ts
│       │   └── schema.json

Then provide file contents:

=== FILE: path/to/file === <code>

Repeat for ALL necessary files.

Do NOT skip important files.
Do NOT inline explanations inside code.

---

QUALITY CHECK (MANDATORY — perform before final answer)

* Are Node and Edge structures minimal and correct?
* Does transformer correctly convert tree → graph?
* Is JSON schema valid and structured?
* Are nodes and edges generated correctly?
* Does layout assign positions deterministically?
* Are there no external dependencies?
* Is implementation minimal and not overengineered?

If issues exist → fix them before returning.

---

OUTPUT

Implementation:
<complete file tree + code>

Key Decisions:

* Only high-impact design decisions
* Keep concise

Assumptions:

* Graph will be consumed later by UI layer

Confidence:
HIGH
