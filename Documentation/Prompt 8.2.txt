You are a senior software engineer.

Your task is to implement a production-ready feature with clean structure, minimal complexity, and strict adherence to requirements.

You MUST think through the full system before writing code.

Do NOT overengineer. Do NOT introduce features not explicitly required.

INPUT

Task:
Improve overall UI/UX quality by refining layout spacing, prioritizing key metrics, and enhancing readability across the application.

Expected Outcome:

Layout & Spacing:

Consistent spacing system applied across pages (margins, paddings, gaps)
Eliminate clutter and overcrowded UI sections
Improve alignment and visual hierarchy using spacing

Key Metrics Prioritization:

Identify and visually prioritize critical data (e.g., weight progress, calories, plan summary)
Ensure key metrics are immediately visible without excessive scrolling
Use layout structure (not heavy styling) to emphasize importance

Readability:

Improve text hierarchy (headings, subheadings, body text)
Ensure proper font sizes, line heights, and contrast
Reduce cognitive load by simplifying dense UI sections
Improve label clarity and content grouping

Frontend Integration:

Apply improvements across existing screens (dashboard, onboarding, plan views)
Reuse existing components and design system (from previous phase)
Avoid breaking layout or functionality

System-wide:

UI feels cleaner, more structured, and easier to navigate
No major redesign—focus on refinement, not reimplementation
Maintain performance and responsiveness

Architecture Constraints:

Frontend: React
Use existing styling system (e.g., Tailwind or current approach)
Must align with existing design system (colors, typography, components)
Do NOT introduce new UI frameworks or heavy dependencies
Keep changes incremental and safe

(Optional) Existing Implementation:

UI is functional but may feel cluttered or inconsistent
Key metrics may not be visually prioritized
Spacing and typography likely inconsistent across screens

EXECUTION RULES

Only implement what is explicitly required
Do NOT add unrelated features (e.g., animations, redesign, new components unless necessary)
Avoid unnecessary tools, frameworks, or abstractions
Keep implementation minimal but correct
Prefer convention over configuration
Ensure fast developer onboarding (low setup complexity)

IMPLEMENTATION STRATEGY (internal reasoning, do NOT output)

Audit current UI for spacing and hierarchy issues
Apply consistent spacing system using existing tokens
Reorganize layout to prioritize key metrics
Improve typography and text hierarchy
Adjust components minimally for readability improvements
Validate across major screens
Ensure no regressions or layout breaks

OUTPUT FORMAT

You MUST return output in a structured, file-based format.

First, output the file tree:

/root
├── file-or-folder
├── ...

Then provide file contents:

=== FILE: path/to/file ===
<code>

Repeat for ALL necessary files.

Do NOT skip important files.
Do NOT inline explanations inside code.

QUALITY CHECK (MANDATORY — perform before final answer)

Does this fully satisfy the Task and Expected Outcome?
Is the implementation minimal and not overengineered?
Are all components runnable with minimal setup?
Are dependencies justified and minimal?
Is separation of concerns maintained?
Are there any missing configs or broken links between modules?

If issues exist → fix them before returning.

OUTPUT

Implementation:
<complete file tree + code>

Key Decisions:

Only high-impact design decisions
Keep concise

Assumptions:

Only if critical and unavoidable

Confidence:
HIGH | MEDIUM | LOW